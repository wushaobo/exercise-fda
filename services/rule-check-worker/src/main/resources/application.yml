server:
  port: 8081

spring:
  application:
    name: rule-check-worker
  threads:
    virtual:
      enabled: true

fds:
  sqs:
    ticket-queue-url: http://localhost:4566/000000000000/fds-ticket-queue
  rules:
    amount-threshold: 10000.0
  redis:
    result-channel: fds:results
    result-ttl-seconds: 300

